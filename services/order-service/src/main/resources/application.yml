server:
  port: 8081

spring:
  application:
    name: order-service

services:
  payment:
    base-url: http://localhost:8082

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics