# Oracle Terraform Layer

This is a placeholder for the future Oracle Cloud bootstrap.

Expected future responsibilities:

- create an OCI VM or OKE cluster;
- configure networking/security rules;
- attach persistent storage where needed;
- install or expose a Kubernetes API;
- output kubeconfig/context information for `infra/terraform/platform`.

Nothing is created by this layer yet.
