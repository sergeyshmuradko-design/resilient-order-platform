# Placeholder for the future Oracle Cloud layer.
#
# This directory should eventually create or attach to the cloud Kubernetes
# runtime: OCI VM with k3s, OKE, networking, firewall rules, disks and outputs
# needed by the platform layer.
#
# It is intentionally empty today so the project can evolve without mixing
# Codespaces-only k3d logic with future cloud infrastructure.
